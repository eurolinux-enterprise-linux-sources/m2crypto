diff -urN M2Crypto/SWIG/_aes.i M2Crypto-0.20.2/SWIG/_aes.i
--- M2Crypto/SWIG/_aes.i	2012-03-15 03:53:09.170193340 +0100
+++ M2Crypto-0.20.2/SWIG/_aes.i	2012-03-15 03:53:24.573160173 +0100
@@ -64,6 +64,7 @@
     const void *buf;
     Py_ssize_t len;
     unsigned char *out;
+    PyObject *res;
 
     if (PyObject_AsReadBuffer(in, &buf, &len) == -1)
         return NULL;
@@ -76,7 +77,9 @@
         AES_encrypt((const unsigned char *)in, out, key);
     else
         AES_decrypt((const unsigned char *)in, out, key);
-    return PyString_FromStringAndSize((char*)out, outlen);
+    res = PyString_FromStringAndSize((char*)out, outlen);
+    PyMem_Free(out);
+    return res;
 }
 
 int AES_type_check(AES_KEY *key) {
